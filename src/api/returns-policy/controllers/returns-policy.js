'use strict';

/**
 * returns-policy controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::returns-policy.returns-policy');
